# Nawal AI - Core Requirements
# NOTE: Heavy ML deps (torch, etc.) are installed separately
# to avoid pip resolution-too-deep errors

# Core ML (CPU-only via PyTorch index in CI/Docker)
torch>=2.0.0
torchvision>=0.15.0

# NLP
transformers>=4.30.0
datasets>=2.14.0
accelerate>=0.21.0
huggingface-hub>=0.16.0

# Federated learning
flwr>=1.5.0

# Blockchain integration
substrate-interface>=1.7.0

# Security & privacy
cryptography>=42.0.0
pynacl>=1.5.0
pycryptodome>=3.19.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0

# Configuration & validation
pyyaml>=6.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Logging
structlog>=23.1.0
rich>=13.0.0
loguru>=0.7.0

# Serialization
msgpack>=1.0.0
orjson>=3.9.0

# Networking
aiohttp>=3.8.0
websockets>=11.0

# gRPC
grpcio>=1.56.0
protobuf>=4.21.0,<5.0.0

# API
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# Monitoring
prometheus-client>=0.17.0
psutil>=5.9.0
Pillow>=10.0.0
